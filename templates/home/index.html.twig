{% extends 'base.html.twig' %}

{% block title %}Hello Home!{% endblock %}

{% block body %}
<h1>HomePage</h1>
<h2>ICI toutes les annonces qui seront publiees </h2>
<ul>TODOLIST
    <li>Appliquer filtre de recherche d'annonce par département et ville :
    Reprise du formulaire qui récupère l'id du département.
    Mise en place d'un bouton à coté département, si ce bouton est clické, cherche dans bdd toutes les annonces
    dont la colonne ville_id  a un code_departement = à celui séléctionné... </li>
</ul>
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; } 
 </style> 
   {% for ad in ads %}
        <div class="ad">
            <h2>{{ ad.title }}</h2>
            <p>{{ ad.text }}</p>
            <p>Date de création : {{ ad.date|date('Y-m-d H:i:s') }}</p>
            <p>Ville : {{ ad.ville.nom }}</p>
            <p>Ville ID : {{ ad.ville.id }}</p>
        </div>
        <hr>
    {% endfor %}
 {% endblock %}  
