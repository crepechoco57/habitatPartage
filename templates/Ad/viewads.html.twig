
{% extends 'base.html.twig' %}

{% block title %}Liste des Annonces{% endblock %}

{% block body %}
    <h1>Liste des Annonces</h1>

    {% for ad in ads %}
        <div class="ad">
            <h2>{{ ad.title }}</h2>
            <p>{{ ad.text }}</p>
            <p>Date de création : {{ ad.date|date('Y-m-d H:i:s') }}</p>
            <p>Ville : {{ ad.ville.nom }}</p>
            <p>Ville ID : {{ ad.ville.id }}</p>
        </div>
        <hr>
    {% endfor %}
{% endblock %}